Add a POST /api/detect endpoint to ReportResource. It should:

- Find all distinct (county, disease) pairs from DiseaseReport in the last 7 days.
- For each pair, call OutbreakDetector.detect(county, disease).
- If an alert is returned, set detectedAt = now, watsonSummary = "Outbreak detected (mock) - further analysis recommended", recommendedAction = "Notify county health officer", then save via OutbreakAlertRepository.
- Return JSON: { "alertsCreated": count }.
