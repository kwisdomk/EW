Generate a multi-stage Dockerfile for the Quarkus backend:

- Stage 1: maven:3.9-eclipse-temurin-17 as builder, copy pom.xml and src, run mvn package.
- Stage 2: openjdk:17-jre-slim, copy --from=builder /target/*-runner.jar /app/app.jar, expose 8080, entrypoint java -jar /app/app.jar.
