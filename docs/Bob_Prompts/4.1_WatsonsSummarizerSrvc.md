Create a Quarkus service `WatsonSummarizer` with method `summarize(county, disease, cases, baseline, zScore)`. If environment variables `WATSON_API_KEY` and `WATSON_URL` are set, call IBM Watson Natural Language Understanding API with a custom text: "In {county}, {disease} cases spiked to {cases} this week, which is {zScore} standard deviations above the baseline of {baseline}. Recommend immediate investigation." Parse the response to extract a summary sentence. If variables not set, return a mock summary: "[MOCK] {disease} outbreak in {county}, {cases} cases vs baseline {baseline}. Action: Alert health department."
