In the Dashboard page, add a Leaflet map centered on Kenya (lat 0.5, lng 37.5, zoom 6). Use a simple GeoJSON of Kenya (I will provide a file `data/kenya_simplified.geojson`). For now, use a placeholder static polygon. Color the polygon based on the highest active alert in that county (fetch from a new endpoint GET /api/map/alerts). Color mapping: GREEN (no alert), YELLOW (zScore 1-2), ORANGE (2-3), RED (>3).
