In the Alerts page, use react-query to fetch GET /api/alerts every 30 seconds. Display alerts as cards showing county, disease, riskScore, alertLevel, watsonSummary, and recommendedAction. Add an "Acknowledge" button that calls POST /api/alerts/{id}/acknowledge and invalidates the query. Use axios for API calls.
