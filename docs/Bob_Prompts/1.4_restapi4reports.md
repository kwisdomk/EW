Generate a REST resource `ReportResource` with:

- POST /api/reports – accepts a DiseaseReport JSON, saves via repository, returns 201 with the saved entity.
- GET /api/reports – returns all reports as JSON.
  Use `@Path("/api")` and `@Produces(MediaType.APPLICATION_JSON)`.
