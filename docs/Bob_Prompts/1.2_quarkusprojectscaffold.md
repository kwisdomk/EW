Generate a Quarkus Maven project in a folder named `backend`. Use groupId `com.epidemiwatch`, artifactId `epidemiwatch-backend`. Include dependencies: resteasy-reactive, jackson, hibernate-orm-panache, postgresql, smallrye-openapi, quarkus-jdbc-h2 (for local dev). Create the standard Maven directory structure.
