Modify the POST /api/detect endpoint: after creating an OutbreakAlert, call WatsonSummarizer.summarize(...) using the actual values, and set the alert's watsonSummary and recommendedAction fields before saving.
